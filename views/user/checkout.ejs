<% var title = "Checkout" %>

<!-- Checkout Page Specific CSS -->
<link rel="stylesheet" href="/css/checkout.css">

<!-- Breadcrumbs - Positioned correctly below navbar -->
<div class="container my-4">
  <%- include('partials/breadcrumb', {
    breadcrumbs: [
      { label: 'Home', href: '/' },
      { label: 'Cart', href: '/cart' },
      { label: 'Checkout' }
    ]
  }) %>
</div>

<!-- Main Checkout Container -->
<div class="container pb-4 checkout-content">
    <!-- Checkout Header - Styled like cart header bar -->
    <div class="checkout-header-bar">
        <h1 class="checkout-title">
            <i class="bi bi-credit-card me-2"></i>Checkout
        </h1>
    </div>

    <!-- Main Content -->
    <div class="main-checkout-content">
        <!-- Left Side - Address and Payment (Vertical Stack) -->
        <div class="checkout-left">
            <!-- Delivery Address Section -->
            <div class="section-card">
                <div class="section-title">
                    <div>
                        <i class="bi bi-geo-alt"></i>
                        Delivery Address
                    </div>
                    <button class="btn-add-new-address" onclick="showAddAddressModal()">
                        <i class="bi bi-plus-circle"></i> Add New Address
                    </button>
                </div>

                <!-- Address Container -->
                <div id="addressContainer">
                    <% if (addresses && addresses.length > 0) { %>
                        <!-- Initially show first 3 addresses -->
                        <% const initialLimit = 3; %>
                        <% addresses.slice(0, Math.min(initialLimit, addresses.length)).forEach((address, index) => { %>
                            <div class="address-card <%= index === 0 ? 'selected' : '' %>" id="address-card-<%= address._id %>" data-address-index="<%= index %>">
                                <label class="d-flex align-items-start">
                                    <input type="radio" 
                                        name="deliveryAddress" 
                                        value="<%= addressDocumentId %>"
                                        data-address-index="<%= index %>" 
                                        <%= index === 0 ? 'checked' : '' %> 
                                        onclick="selectAddress(this)">
                                    <div class="address-info flex-grow-1">
                                        <h6><%= address.name %></h6>
                                        <p><%= address.addressType %></p>
                                        <p><%= address.landMark %></p>
                                        <p><%= address.city %>, <%= address.state %></p>
                                        <p>PIN: <%= address.pincode %></p>
                                        <p><i class="bi bi-telephone"></i> <%= address.phone %></p>
                                        <% if (address.altPhone) { %>
                                            <p><i class="bi bi-telephone"></i> <%= address.altPhone %> (Alt)</p>
                                        <% } %>
                                    </div>
                                    <div class="d-flex gap-2">
                                        <button class="btn btn-sm btn-outline-secondary" onclick="editAddress('<%= address._id %>')">
                                            <i class="bi bi-pencil"></i>
                                        </button>
                                        <button class="btn btn-sm btn-outline-danger" onclick="deleteAddress('<%= address._id %>')">
                                            <i class="bi bi-trash"></i>
                                        </button>
                                    </div>
                                </label>
                            </div>
                        <% }) %>
                        
                        <!-- Hidden addresses (will be shown on load more) -->
                        <% if (addresses.length > initialLimit) { %>
                            <% addresses.slice(initialLimit).forEach((address, index) => { %>
                                <% const actualIndex = index + initialLimit; %>
                                <div class="address-card hidden-address" 
                                    id="address-card-<%= address._id %>" 
                                    data-address-index="<%= actualIndex %>" 
                                    style="display: none;">
                                    <label class="d-flex align-items-start">
                                        <input type="radio" 
                                            name="deliveryAddress" 
                                            value="<%= addressDocumentId %>" 
                                            data-address-index="<%= actualIndex %>" 
                                            onclick="selectAddress(this)">
                                        <div class="address-info flex-grow-1">
                                            <h6><%= address.name %></h6>
                                            <p><%= address.addressType %></p>
                                            <p><%= address.landMark %></p>
                                            <p><%= address.city %>, <%= address.state %></p>
                                            <p>PIN: <%= address.pincode %></p>
                                            <p><i class="bi bi-telephone"></i> <%= address.phone %></p>
                                            <% if (address.altPhone) { %>
                                                <p><i class="bi bi-telephone"></i> <%= address.altPhone %> (Alt)</p>
                                            <% } %>
                                        </div>
                                        <div class="d-flex gap-2">
                                            <button class="btn btn-sm btn-outline-secondary" onclick="editAddress('<%= address._id %>')">
                                                <i class="bi bi-pencil"></i>
                                            </button>
                                            <button class="btn btn-sm btn-outline-danger" onclick="deleteAddress('<%= address._id %>')">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        </div>
                                    </label>
                                </div>
                            <% }) %>
                            
                            <!-- Load More Button -->
                            <div class="load-more-container">
                                <span id="loadMoreAddressesBtn" class="load-more-span" onclick="loadMoreAddresses()">
                                    <i class="bi bi-arrow-down-circle me-2"></i>Load More Addresses (<span id="remainingCount"><%= addresses.length - initialLimit %></span>)
                                </span>
                            </div>
                        <% } %>
                    <% } else { %>
                        <div class="text-center py-4">
                            <p class="text-muted">No addresses found. Please add a delivery address to continue.</p>
                            <button class="btn btn-primary" onclick="showAddAddressModal()">
                                <i class="bi bi-plus-circle"></i> Add Your First Address
                            </button>
                        </div>
                    <% } %>
                </div>
            </div>

            
            <!-- Coupon Section -->
            <div class="section-card">
                <div class="section-title">
                    <div><i class="bi bi-tag"></i> Apply Coupon</div>
                </div>
                
                <!-- Coupon Input Form -->
                <div id="coupon-form" style="<%= typeof appliedCoupon !== 'undefined' && appliedCoupon ? 'display: none;' : '' %>">
                    <div class="input-group">
                        <input type="text" id="couponCode" class="form-control" placeholder="Enter coupon code" maxlength="20" style="text-transform: uppercase;">
                        <button type="button" id="applyCouponBtn" class="btn btn-primary">Apply Coupon</button>
                    </div>
                    
                    <!-- View Available Coupons Link -->
                    <div class="view-coupons-container">
                        <span class="view-available-coupons" id="viewAvailableCouponsBtn">
                            View Available Coupons
                        </span>
                    </div>
                </div>

                <!-- Applied Coupon Display -->
                <div id="applied-coupon" style="<%= typeof appliedCoupon === 'undefined' || !appliedCoupon ? 'display: none;' : '' %>">
                    <div class="alert alert-success d-flex justify-content-between align-items-center">
                        <div>
                            <strong id="coupon-name"><%= typeof appliedCoupon !== 'undefined' && appliedCoupon ? appliedCoupon.name : '' %></strong>
                            <br>
                            <small>Code: <span id="coupon-code"><%= typeof appliedCoupon !== 'undefined' && appliedCoupon ? appliedCoupon.code : '' %></span></small>
                            <br>
                            <small>Discount: ₹<span id="coupon-discount"><%= typeof appliedCoupon !== 'undefined' && appliedCoupon ? appliedCoupon.discountAmount : '0' %></span></small>
                        </div>
                        <button type="button" id="removeCouponBtn" class="btn btn-sm btn-outline-danger">
                            Remove
                        </button>
                    </div>
                </div>
            </div>

            <!-- Payment Method Section -->
            <div class="section-card">
                <div class="section-title">
                    <div>
                        <i class="bi bi-credit-card"></i>
                        Payment Method
                    </div>
                </div>

                <!-- Credit/Debit Card -->
                <div class="payment-option">
                    <label class="d-flex align-items-center">
                        <input type="radio" name="paymentMethod" value="card" onclick="selectPayment(this)">
                        <div class="payment-icon">
                            <i class="bi bi-credit-card"></i>
                        </div>
                        <div class="payment-info">
                            <h6>Credit/Debit Card</h6>
                            <p>Visa, MasterCard, American Express</p>
                        </div>
                    </label>
                </div>

                <!-- UPI Payment (Razorpay) -->
                <div class="payment-option">
                    <label class="d-flex align-items-center">
                        <input type="radio" name="paymentMethod" value="upi" onclick="selectPayment(this)">
                        <div class="payment-icon">
                            <i class="bi bi-phone"></i>
                        </div>
                        <div class="payment-info">
                            <h6>UPI Payment</h6>
                            <p>Pay using Google Pay, PhonePe, Paytm & more</p>
                        </div>
                    </label>
                </div>

                <!-- PayPal Payment -->
                <div class="payment-option">
                    <label class="d-flex align-items-center">
                        <input type="radio" name="paymentMethod" value="paypal" onclick="selectPayment(this)">
                        <div class="payment-icon">
                            <i class="bi bi-paypal"></i>
                        </div>
                        <div class="payment-info">
                            <h6>PayPal</h6>
                            <p>Pay with your PayPal account or cards</p>
                        </div>
                    </label>
                </div>

                <!-- Net Banking -->
                <div class="payment-option">
                    <label class="d-flex align-items-center">
                        <input type="radio" name="paymentMethod" value="netbanking" onclick="selectPayment(this)">
                        <div class="payment-icon">
                            <i class="bi bi-bank"></i>
                        </div>
                        <div class="payment-info">
                            <h6>Net Banking</h6>
                            <p>Pay directly from your bank account</p>
                        </div>
                    </label>
                </div>

                <!-- ✅ UPDATED: Wallet Payment with proper status -->
                <div class="payment-option" id="wallet-payment-option" <%= walletBalance < total ? 'style="opacity: 0.7;"' : '' %>>
                    <label class="d-flex align-items-center">
                        <input type="radio" name="paymentMethod" value="wallet" onclick="selectPayment(this)" 
                            <%= walletBalance < total ? 'disabled' : '' %>>
                        <div class="payment-icon">
                            <i class="bi bi-wallet2"></i>
                        </div>
                        <div class="payment-info">
                            <h6>Wallet Payment</h6>
                            <p id="wallet-balance-text" class="<%= walletBalance >= total ? 'text-success' : 'text-warning' %>">
                                Available: <strong>₹<%= walletBalance.toLocaleString('en-IN') %></strong>
                            </p>
                        </div>
                    </label>
                    <!-- ✅ Insufficient Balance Warning -->
                    <div id="insufficient-balance-warning" class="mt-2 alert alert-warning <%= walletBalance >= total ? 'd-none' : '' %>">
                        <i class="bi bi-exclamation-triangle me-2"></i>
                        <span id="insufficient-balance-message">
                            Insufficient wallet balance. Need ₹<%= (total - walletBalance).toLocaleString('en-IN') %> more.
                        </span>
                        <a href="/profile/wallet" class="alert-link ms-2">Add money to wallet</a>
                    </div>
                </div>

                <!-- Cash on Delivery -->
                <div class="payment-option" id="cod-payment-option">
                    <label class="d-flex align-items-center">
                        <input type="radio" name="paymentMethod" value="cod" onclick="selectPayment(this)">
                        <div class="payment-icon">
                            <i class="bi bi-cash-coin"></i>
                        </div>
                        <div class="payment-info">
                            <h6>Cash on Delivery</h6>
                            <p>Pay when your order is delivered</p>
                        </div>
                    </label>
                    
                    <!-- COD Warning Message -->
                    <div id="cod-warning-message" class="mt-2 alert alert-warning" style="display: none;">
                        <i class="bi bi-exclamation-triangle me-2"></i>
                        <span class="cod-warning-text">
                            Cash on Delivery is not available for orders ₹10,000 or above. Please select an online payment method.
                        </span>
                    </div>
                </div>
            </div>

            <!-- Containers for payment gateways -->
            <div id="paypal-btn-container-left" class="mt-3 d-none"></div>
            <div id="razorpay-btn-container-left" class="mt-3 d-none"></div>
        </div>

        <!-- Right Side - Order Summary -->
        <div class="checkout-right">
            <div class="order-summary">
                <!-- Summary Header -->
                <div class="summary-header">
                    <h5 class="summary-title">
                        <i class="bi bi-receipt me-2"></i>Order Summary
                    </h5>
                </div>

                <!-- Summary Content -->
                <div class="summary-content">
                    <div class="section-subtitle">ITEMS IN CART (<%= totalItemCount || 0 %>)</div>

                    <!-- Order Items -->
                    <% if (cartItems && cartItems.length > 0) { %>
                        <% cartItems.forEach(item => { %>
                            <div class="order-item">
                                <% 
                                    let imageSrc = '/images/placeholder.svg';
                                    let productName = 'Product Name';
                                    
                                    if (item.productId && item.productId.mainImage) {
                                        imageSrc = item.productId.mainImage.startsWith('/') ? 
                                                   item.productId.mainImage : 
                                                   '/uploads/products/' + item.productId.mainImage;
                                        productName = item.productId.productName || 'Product Name';
                                    }
                                    else if (item.product && item.product.mainImage) {
                                        imageSrc = item.product.mainImage.startsWith('/') ? 
                                                   item.product.mainImage : 
                                                   '/uploads/products/' + item.product.mainImage;
                                        productName = item.product.productName || 'Product Name';
                                    }
                                    else if (item.productId && item.productId.subImages && item.productId.subImages.length > 0) {
                                        const subImage = item.productId.subImages[0];
                                        imageSrc = subImage.startsWith('/') ? 
                                                   subImage : 
                                                   '/uploads/products/' + subImage;
                                        productName = item.productId.productName || 'Product Name';
                                    }
                                    else if (item.product && item.product.subImages && item.product.subImages.length > 0) {
                                        const subImage = item.product.subImages[0];
                                        imageSrc = subImage.startsWith('/') ? 
                                                   subImage : 
                                                   '/uploads/products/' + subImage;
                                        productName = item.product.productName || 'Product Name';
                                    }
                                %>
                                <img src="<%= imageSrc %>" 
                                     alt="<%= productName %>" 
                                     class="item-image" 
                                     onerror="this.onerror=null; this.src='/images/placeholder.svg';"
                                     loading="lazy">
                                <div class="item-details">
                                    <h6><%= item.productId.productName %></h6>
                                    <p>Size: <%= item.size %> | Qty: <%= item.quantity %></p>
                                </div>
                                <div class="item-price">₹<%= Math.round(item.totalPrice) %></div>
                            </div>
                        <% }) %>
                    <% } else { %>
                        <div class="order-item">
                            <img src="/images/placeholder.svg" alt="Product" class="item-image">
                            <div class="item-details">
                                <h6>No items in cart</h6>
                                <p>Please add items to continue</p>
                            </div>
                            <div class="item-price">₹0</div>
                        </div>
                    <% } %>

                    <!-- Price Breakdown -->
                    <div class="price-breakdown">
                        <div class="summary-row">
                            <span class="price-label">Subtotal (<%= totalItemCount || 0 %> items)</span>
                            <span class="price-value">₹<%= subtotal || 0 %></span>
                        </div>
                    
                        <!-- Sale Price Discount -->
                        <% if (totalDiscount && totalDiscount > 0) { %>
                            <div class="summary-row discount-row">
                                <span class="price-label">
                                    <i class="bi bi-tag me-1"></i>Product Discount
                                </span>
                                <span class="price-value text-success">-₹<%= totalDiscount %></span>
                            </div>
                        <% } %>
                    
                        <!-- Coupon Discount Row -->
                        <div id="coupon-discount-row" class="summary-row discount-row" style="display: <%= (appliedCoupon && couponDiscount > 0) ? 'flex' : 'none' %>;">
                            <span class="price-label">
                                Coupon Discount 
                                <span id="coupon-code-label">
                                    <% if (appliedCoupon && appliedCoupon.code) { %> 
                                        (<%= appliedCoupon.code %>)
                                    <% } %>
                                </span>
                            </span>
                            <span class="price-value text-success">-₹<span id="discount-amount"><%= couponDiscount || 0 %></span></span>
                        </div>
                    
                        <div class="summary-row">
                            <span class="price-label">
                                <i class="bi bi-truck me-1"></i>Shipping
                            </span>
                            <span class="price-value">
                                <% if (shipping && shipping > 0) { %>
                                    ₹<%= shipping %>
                                <% } else { %>
                                    <span class="text-success fw-bold">FREE</span>
                                <% } %>
                            </span>
                        </div>

                        <div class="price-divider"></div>

                        <div class="summary-row total">
                            <span class="price-label">Total</span>
                            <span class="price-value">₹<span id="final-total"><%= total || 0 %></span></span>
                        </div>
                    </div>

                    <!-- ✅ PLACE ORDER BUTTON - Only in order summary -->
                    <button class="btn-continue btn-disabled" id="placeOrderBtn" onclick="proceedToPayment()" disabled>
                        <i class="bi bi-check-circle"></i> PLACE ORDER
                    </button>

                    <!-- PayPal buttons container -->
                    <div id="paypal-btn-container-main" class="mt-3 d-none"></div>

                    <!-- Back to Cart -->
                    <div class="text-center mt-3">
                        <a href="/cart" class="text-muted text-decoration-none">
                            <i class="bi bi-arrow-left"></i> Back to Cart
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Add/Edit Address Modal -->
<div class="modal fade" id="addressModal" tabindex="-1" aria-labelledby="addressModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="addressModalLabel">
          <i class="bi bi-plus me-2"></i> Add New Address
        </h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <%- include('partials/addresses') %>
      </div>
    </div>
  </div>
</div>

<!-- Coupons Modal -->
<%- include('partials/checkout-coupons-modal') %>

<!-- Geoapify Autocomplete Script -->
<script src="https://api.geoapify.com/v1/api.js?apiKey=<%= geoapifyApiKey %>"></script>

<!-- SweetAlert2 JavaScript -->
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

<script src="/js/user/checkout.js"></script>

<script>
  // Pass server-side data to client-side JavaScript
//   window.geoapifyApiKey = '<%= geoapifyApiKey %>';
  window.orderTotal = <%= total || 0 %>;
  window.walletBalance = <%= walletBalance || 0 %>;
</script>

<!-- Razorpay Checkout Script -->
<script src="https://checkout.razorpay.com/v1/checkout.js"></script>

<script>
  /* ========= PayPal JS-SDK lazy loader ========= */
  window.loadPayPalSdk = function () {
    if (window.paypal) return;
    const s = document.createElement('script');
    s.src = 'https://www.paypal.com/sdk/js?client-id=<%= paypalClientId %>&currency=USD&intent=capture&disable-funding=credit,card';
    s.onload = () => console.log('PayPal SDK loaded');
    s.onerror = (error) => console.error('PayPal SDK failed to load:', error);
    document.head.appendChild(s);
  };
</script>
